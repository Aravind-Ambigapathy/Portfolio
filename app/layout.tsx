import Navbar from '../components/Navbar';

const Layout = () => {
  return (
    <div>
      <Navbar />
      <main>
        {/* rest of the code remains the same */}
      </main>
    </div>
  );
};

export default Layout;